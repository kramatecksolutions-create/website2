import React,{useState} from 'react';import{Link}from'react-router-dom';import{ChevronDown,Menu,X}from'lucide-react';import{services,industries}from'../data/siteData';
export default function Header(){const[open,setOpen]=useState(false);return <header className="site-header"><div className="nav-wrap"><Link to="/" className="brand"><img src="/assets/krama-logo-header.png" alt="KramaTek"/></Link><nav className="desktop-nav"><div className="nav-group"><button>Services <ChevronDown size={14}/></button><div className="mega"><div><div className="mega-label">AI & Data</div>{['ai-solutions','data-engineering','analytics-intelligence'].map(k=><Link key={k} to={`/services/${k}`}><strong>{services[k].title}</strong><span>{services[k].description}</span></Link>)}</div><div><div className="mega-label">Consulting & Operations</div>{['digital-transformation','staff-augmentation','managed-services'].map(k=><Link key={k} to={`/services/${k}`}><strong>{services[k].title}</strong><span>{services[k].description}</span></Link>)}</div></div></div><div className="nav-group"><button>Industries <ChevronDown size={14}/></button><div className="mega industries">{Object.entries(industries).map(([k,v])=><Link key={k} to={`/industries/${k}`}><strong>{v.title}</strong><span>{v.description}</span></Link>)}</div></div><Link to="/platform">Platform</Link><Link to="/why-kramatek">Why KramaTek</Link><Link to="/insights">Insights</Link><Link to="/contact">Contact</Link></nav><Link className="talk-btn" to="/contact">Let's Talk</Link><button className="menu-btn" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<div className="mobile-nav">{Object.entries(services).map(([k,v])=><Link onClick={()=>setOpen(false)} key={k} to={`/services/${k}`}>{v.title}</Link>)}<Link onClick={()=>setOpen(false)} to="/platform">Platform</Link><Link onClick={()=>setOpen(false)} to="/contact">Contact</Link></div>}</header>}
