import React from'react';import{motion}from'framer-motion';import{ArrowRight}from'lucide-react';import{Link}from'react-router-dom';import PageHero from'../components/PageHero';export default function ServicePage({data}){return <><PageHero eyebrow={data.eyebrow} title={data.title} description={data.description}/><section className="section warm"><div className="container"><div className="section-head"><div><div className="eyebrow">CAPABILITIES</div><h2>What we deliver.</h2></div><p>{data.description}</p></div><div className="cards">{data.capabilities.map(([t,d],i)=><motion.article className="service-card" key={t} initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}}><span className="card-num">0{i+1}</span><h3>{t}</h3><p>{d}</p></motion.article>)}</div></div></section><section className="section"><div className="container"><div className="section-head"><div><div className="eyebrow">DELIVERY APPROACH</div><h2>From idea to dependable operation.</h2></div><p>Each engagement is sized around the problem, data, systems, security needs, and delivery constraints.</p></div><div className="steps">{['Discover','Architect','Prototype','Integrate','Operate'].map((s,i)=><div className="step" key={s}><span>0{i+1}</span><h3>{s}</h3><p>Focused decisions, measurable validation, and clear ownership at every stage.</p></div>)}</div></div></section><section className="cta-section"><div className="container cta-box"><div><h2>Explore {data.title.toLowerCase()} with KramaTek.</h2><p>Let’s define the use case, architecture, integrations, and success criteria together.</p></div><Link className="btn gold" to="/contact">Start a conversation <ArrowRight size={17}/></Link></div></section></>}
